CREATE TABLE t1 (n INTEGER, log INTEGER);
BEGIN TRANSACTION;
INSERT INTO t1 VALUES (-1, 1);
INSERT INTO t1 VALUES (3, -9223372036854775808);
INSERT INTO t1 VALUES (4, 1);
INSERT INTO t1 VALUES (4, 9223372036854775807);
SELECT MIN(n), MIN(log), MAX(n), MAX(log), SUM(n), SUM(log), AVG(COALESCE(n, 0)), AVG(log) FROM t1;
SELECT log, AVG(n) + 1 FROM t1 GROUP BY log ORDER BY log;
SELECT log * 2 + 1 AS x, COUNT(*) AS y FROM t1 GROUP BY x ORDER BY 10 - (x + y);
SELECT log, COUNT(*) FROM t1 GROUP BY log HAVING COUNT(*) >= 4 ORDER BY log;
SELECT log, COUNT(*), AVG(n), MAX(n + log * 2) FROM t1 GROUP BY log ORDER BY MAX(n + log * 2) + 0, MIN(log, AVG(n)) + 0;